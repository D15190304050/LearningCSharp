<Window x:Class="ConcurrentWpf.ProgressPercentage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConcurrentWpf"
        mc:Ignorable="d"
        Title="ProgressPercentage" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Name="txtProgress" HorizontalAlignment="Center" VerticalAlignment="Center">0%</TextBlock>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button Name="cmdStart" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" Margin="5" Click="cmdStart_Click">Start</Button>
            <Button Name="cmdCancel" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" Margin="5" Click="cmdCancel_Click">Cancel</Button>
        </Grid>
    </Grid>
</Window>
